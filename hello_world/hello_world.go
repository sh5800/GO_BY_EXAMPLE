package main    //package where the execution code is present

import(         //required imports
	"fmt"       //fmt is used for standard input and output operations
)

func main(){     //the entry point of a go program
	fmt.Println("Hello World!")      //packagename.FunctionName(arguments), here Println is a function in fmt package, Println prints to standard output i.e the console and and moves the cursor to the next line.
}